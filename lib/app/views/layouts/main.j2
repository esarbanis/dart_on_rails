<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Dart on Rails Blog</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,typography"></script>
  </head>

  
<body>

  <header class="absolute inset-x-0 top-0 z-50">
      <nav class="flex items-center justify-between p-6 lg:px-8" aria-label="Global">
        <div class="flex lg:flex-1">
          <a href="/" class="-m-1.5 p-1.5">
            <span class="sr-only">Dart on Rails Blog</span>
            <img class="h-8 w-auto" src="/assets/images/logo.svg" alt="">
          </a>
        </div>
        {% if user  %}
          <div class="flex flex-1 items-center justify-end gap-x-6">
              <a href="/users/logout" class="text-sm font-semibold leading-6 text-gray-900">Log out</a>
              <a href="/posts/new" class="rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">New Post</a>
          </div>
        {% else %}
          <div class="hidden lg:flex lg:flex-1 lg:justify-end">
            <a href="/users/login" class="text-sm font-semibold leading-6 text-gray-900">Log in <span aria-hidden="true">&rarr;</span></a>
          </div>
        {% endif %}
      </nav>
  </header>

	<div class="isolate bg-white px-6 py-24 sm:py-8 lg:px-8">
    <div class="mx-auto max-w-2xl text-center">
      <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">Dart on Rails Blog</h2>
      <p class="mt-2 text-lg leading-8 text-gray-600">Learn more about how to use rails paradigm with dart.</p>
    </div>
  </div>

  <div class="mx-auto mt-10 max-w-lg space-y-16">
		{% block content %}
    {% endblock %}
	</div>
</body>

</html>
