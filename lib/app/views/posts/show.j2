{% extends "layouts/main.j2" %}
{% block content %}
    <div class="bg-white px-6 py-8 lg:px-8">
      <div class="mx-auto max-w-3xl text-base leading-7 text-gray-700">
        <a href="/posts" class="rounded-full bg-white px-3.5 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50">Back</a>
        <h1 class="mt-2 text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">{{post.title}}</h1>
        <div class="mt-10 max-w-2xl">
          <p>{{post.content}}</p>
        </div>
      </div>
  </div>
{% endblock %}